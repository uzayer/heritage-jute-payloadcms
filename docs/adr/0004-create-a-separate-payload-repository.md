# Create a separate Payload repository

The Payload application will move to a sibling directory and be initialized as its own Git repository with a clean history. The existing Astro repository and detached pre-Astro Next.js worktree remain preserved as migration references, rather than contributing obsolete framework and CloudCannon history to the new deployment unit.
